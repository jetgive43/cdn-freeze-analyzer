# Create React App: keep the dev server on HTTP so browser probes can use http:// targets.
# Copy to .env (do not commit .env). Do not set HTTPS=true unless you accept mixed-content limits.
HTTPS=false

# Optional: API on another origin (e.g. UI http://194.120.230.185:3000 and API :5000). Rebuild after change.
# REACT_APP_API_BASE_URL=http://194.120.230.185:5000
